import { Inter, Afacad, DM_Sans } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const afacad = Afacad({ subsets: ['latin'] });

export const dmSans = DM_Sans({ subsets: ['latin']})